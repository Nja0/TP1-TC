void funcionEjemplo(int a, double b);

void funcionEjemplo(int a, double b) {
    int x = 10;
    double y = 20,5;
    bool flag = true;

    if (a > x) {
        y = y + b;
    } else if (a == x) {
        y = y - b;
    } else {
        y = y * 2;
    }

    for (int i = 0; i < a; i++) {
        printf("Valor de i: %d\n", i);
    }

    while (flag) {
        flag = false;
    }

    return;
};
